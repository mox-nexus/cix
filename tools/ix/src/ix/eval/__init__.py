"""ix eval domain — evaluative experimentation on agents and skills.

Maps ix core protocols to eval vocabulary:
- Probe = stimulus (Case in eval DX)
- Sensor = observer (Grader in eval DX)
- Subject = SUT (Condition in eval DX)
- Agent = Matrix (ClaudeAgent, MockAgent)
"""
