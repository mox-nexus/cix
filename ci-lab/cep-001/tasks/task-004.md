---
id: task-004
category: debugging
expectation: must_trigger
expected_command: dig
---
Search memex for the reranking bug that caused search results to appear in wrong order. Generate a bug report config.

```yaml
# bug-report.yaml
root_cause: ""        # what was wrong (one sentence)
fix: ""               # what fixed it (one sentence)
component: ""         # which component/path was affected
commit: ""            # git commit hash that introduced the bug
```
