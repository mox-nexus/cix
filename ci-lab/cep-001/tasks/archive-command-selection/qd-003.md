---
id: qd-003
boundary: query-dig
expectation: must_trigger
expected_command: query
expected_query: "SELECT source, strftime(created, '%Y-%m') AS month, COUNT(*) FROM fragments GROUP BY source, month"
cues:
  toward_correct: "aggregation query, counts, grouping — pure SQL territory"
  toward_incorrect: "dig cannot aggregate or count, only retrieves fragments"
---
How many fragments do I have per source, grouped by month? Give me the counts.
